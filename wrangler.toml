# Cloudflare Pages — use `npm run deploy:pages` as the deploy command in the dashboard.
# Do NOT use `wrangler deploy` (that targets Workers, not Pages static assets).

name = "wc2026-stickers"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
