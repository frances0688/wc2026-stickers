# Static Vite app — deploy with: npm run deploy:pages  (runs wrangler deploy)
# Requires Node 22+ in Cloudflare build settings.

name = "wc2026-stickers"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
